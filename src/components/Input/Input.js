import React from "react";

const Input = ({
  value,
  onChange,
  name,
  onBlur,
  placeholder,
  type = "text",
  error,
  touched,
  customClass,
  serverError,
}) => {
  return <input type="type"></input>;
};

export default Input;
